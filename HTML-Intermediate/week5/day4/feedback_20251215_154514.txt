URL: https://github.com/orkessler1/D1-BOOTCAMP-STAGE1/blob/main/HTML-Intermediate/week5/day4/class1.ts
suggestions for improvement:
- The code does not correctly address any of the exercises provided. It appears to be a collection of unrelated TypeScript snippets. Each exercise has a specific task, and the submitted code does not fulfill those tasks.
- The code needs to be restructured to address each exercise separately and correctly.
- Remove the unrelated snippets of code to improve readability and focus on the exercise requirements.
- For Exercise 1, the code should define Person and Address types and then create an intersection type combining them.
- For Exercise 2, the code should define a function that uses type guards to determine if the input is a number or a string.
- For Exercise 3, the code should create a variable of type 'any' and cast it to a string.
- For Exercise 4, the code should define a function that takes an array of number | string and uses type assertions to return the first element as a string.
- For Exercise 5, the code should define a generic function that takes a parameter constrained to types with a length property.
- For Exercise 6, the code should define Employee, Person, and Job types, and use type guards within the describeEmployee function.
- For Exercise 7, the code should define a generic function that uses type assertions to treat the input as a string after ensuring it has a toString() method.
Correctness: 10.0%
-----
URL: https://github.com/orkessler1/D1-BOOTCAMP-STAGE1/blob/main/HTML-Intermediate/week5/day4/class2.ts
suggestions for improvement:
- The provided code does not directly address any of the specific tasks outlined in the exercises. It includes examples of generics, but they are not used in the context of intersection types, type guards, type casting, type assertions, or generic constraints as the exercises require.
- The code includes unrelated function `getRandomElement` which wasn't asked for in the exercises
Correctness: 0%
-----
URL: https://github.com/orkessler1/D1-BOOTCAMP-STAGE1/blob/main/HTML-Intermediate/week5/day4/exe1.ts
suggestions for improvement:
- Consider adding more comments in English for better understanding for a broader audience.
- The examples could be integrated with console.log to print out the results for better visibility.
Correctness: 100%
-----